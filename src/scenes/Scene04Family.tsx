import { MemoryGallery } from '../experience/MemoryGallery'; import { RecurringTable } from '../experience/RecurringTable'; import { useExperienceStore } from '../stores/useExperienceStore';
export const Scene04Family = () => { const beat = useExperienceStore(s => s.beat); const night = beat >= 6; return <group><RecurringTable />{(beat === 0 || beat === 1) && <MemoryGallery chapters={['wedding']} radius={3.1} />}{(beat === 3 || beat === 4) && <MemoryGallery chapters={['family']} radius={3.5} />}
  <ambientLight intensity={night ? .12 : .55} color={night ? '#23345c' : '#ffe6b3'} /><pointLight position={[0,2.4,1]} color="#ffc979" intensity={night ? 1.1 : 3} /></group>; };
